/**
 * represent a search_algo that use heuristic function
 */
public interface heuristic_search_algo  extends search_algo{


    public void set_heuristic_func(heuristic_function b);
    ollections.sort(list, new Comparator<MyClass>(){
        public int compare(MyClass a, MyClass b)
        {
            // compare objects
        }
    });
}